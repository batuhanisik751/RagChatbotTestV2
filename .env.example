# Required: your OpenAI API key
OPENAI_API_KEY=

# Optional: GitHub personal access token (raises rate limit from 60 to 5000 req/hr)
# GITHUB_TOKEN=

# Also fill in persona.yaml with your personal details (name, role, bio, etc.).
# The github and linkedin fields in persona.yaml are used by the agent tools.
